# Approvals

Approval is required before a sensitive or externally visible effect, not
afterward. The request identifies the operation and the material arguments the
user is authorizing.

Approval is scoped to that operation unless the Host exposes a separate,
explicitly understood durable policy. Cancellation, timeout, argument changes,
or session changes invalidate a pending approval.
