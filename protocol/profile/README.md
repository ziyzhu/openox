# Profile Contract

This surface owns the portable Profile directory, identifiers, chats, turns,
skills, artifacts, versioning, and forward-compatibility rules. The current
storage contract is in `docs/STORAGE.md`.
