# OpenOx Protocol

Code is the source of truth.

- `schema.ts` defines shared values.
- `host.ts` defines development Host VM control.
- `profile.ts` defines portable Profile and chat metadata.
- `services.ts` defines service repositories and manifests.

Behavior not encoded here belongs to the implementation and its tests until it has a machine-readable protocol contract.
