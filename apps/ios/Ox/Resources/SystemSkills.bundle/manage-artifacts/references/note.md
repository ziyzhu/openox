# Note

Write or update one UTF-8 Markdown file at `artifacts/<name>.md`. Use a short meaningful filename, preserve the user's content, and add only useful structure. Read an existing note before revising it and prefer `ox.fs.edit` for targeted changes. The successful write or edit displays the note in the conversation automatically.
